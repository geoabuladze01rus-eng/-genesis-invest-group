document.querySelectorAll('.portal-nav a').forEach(a=>a.addEventListener('click',()=>{document.querySelectorAll('.portal-nav a').forEach(x=>x.classList.remove('active'));a.classList.add('active')}));const form=document.getElementById('investorApplication');if(form){form.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(form);const draft={name:f.get('name')||'',contact:f.get('contact')||'',project:f.get('project')||'',ticket:f.get('ticket')||'',note:f.get('note')||'',createdAt:new Date().toISOString()};try{sessionStorage.setItem('genesisInvestorApplication',JSON.stringify(draft))}catch(_){ }const status=document.getElementById('applicationStatus');if(status)status.textContent='Черновик заявки сформирован локально в этой сессии. Для реального доступа требуется screening и NDA.';setTimeout(()=>{location.href='./investor-access.html'},1400)})}const sections=[...document.querySelectorAll('.portal-section[id],#overview')];const nav=[...document.querySelectorAll('.portal-nav a')];if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+visible.target.id))},{rootMargin:'-35% 0px -55% 0px',threshold:[0,.2,.5,1]});sections.forEach(s=>observer.observe(s))}