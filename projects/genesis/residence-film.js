(()=>{const body=document.querySelector('.res-film');if(!body)return;const sections=[...document.querySelectorAll('main > section')];if(!sections.length)return;let current=0,tourOn=false,voiceOn=false,timer=null;const dock=document.createElement('div');dock.className='rf-tourbar';dock.innerHTML='<span class="rf-tourlabel">INTERACTIVE RESIDENCE TOUR</span><div class="rf-touractions"><button data-tour>Start tour</button><button data-voice aria-pressed="false">Voice off</button><button data-prev>←</button><button data-next>→</button><button data-full>Fullscreen</button></div><span class="rf-tourstatus">01 / '+String(sections.length).padStart(2,'0')+'</span>';document.body.appendChild(dock);const tourBtn=dock.querySelector('[data-tour]'),voiceBtn=dock.querySelector('[data-voice]'),status=dock.querySelector('.rf-tourstatus'),fullBtn=dock.querySelector('[data-full]');function voice(){if(!voiceOn||!('speechSynthesis'in window))return;speechSynthesis.cancel();const s=sections[current],headline=s.querySelector('h1,h2,h3'),text=s.querySelector('p');if(!headline&&!text)return;const clean=(headline?.textContent||'')+'. '+(text?.textContent||'');const u=new SpeechSynthesisUtterance(clean);u.lang='ru-RU';u.rate=.9;u.pitch=.83;const voices=speechSynthesis.getVoices().filter(v=>/^ru/i.test(v.lang));const hints=['Maxim','Maksim','Yuri','Dmitry','Alexander','Максим','Юрий','Дмитрий','Александр'];u.voice=voices.find(v=>hints.some(h=>v.name.toLowerCase().includes(h.toLowerCase())))||voices[0]||null;speechSynthesis.speak(u)}function setCurrent(i,scroll=true){current=(i+sections.length)%sections.length;sections.forEach((s,n)=>s.classList.toggle('rf-current',n===current));status.textContent=String(current+1).padStart(2,'0')+' / '+String(sections.length).padStart(2,'0');if(scroll)sections[current].scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'});setTimeout(voice,450)}function stopTour(){tourOn=false;clearInterval(timer);tourBtn.textContent='Start tour'}function startTour(){tourOn=true;tourBtn.textContent='Stop tour';setCurrent(current);clearInterval(timer);timer=setInterval(()=>{if(current===sections.length-1){stopTour();return}setCurrent(current+1)},9000)}tourBtn.addEventListener('click',()=>tourOn?stopTour():startTour());dock.querySelector('[data-prev]').addEventListener('click',()=>{stopTour();setCurrent(current-1)});dock.querySelector('[data-next]').addEventListener('click',()=>{stopTour();setCurrent(current+1)});voiceBtn.addEventListener('click',()=>{voiceOn=!voiceOn;voiceBtn.textContent=voiceOn?'Voice on':'Voice off';voiceBtn.setAttribute('aria-pressed',String(voiceOn));if(!voiceOn&&'speechSynthesis'in window)speechSynthesis.cancel();else voice()});fullBtn.addEventListener('click',async()=>{try{if(!document.fullscreenElement){await document.documentElement.requestFullscreen()}else await document.exitFullscreen()}catch(e){}});document.addEventListener('fullscreenchange',()=>fullBtn.textContent=document.fullscreenElement?'Exit fullscreen':'Fullscreen');const observer=new IntersectionObserver(entries=>{if(tourOn)return;const best=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!best)return;const i=sections.indexOf(best.target);if(i>=0){current=i;status.textContent=String(i+1).padStart(2,'0')+' / '+String(sections.length).padStart(2,'0')}},{threshold:[.25,.5,.75]});sections.forEach(s=>{observer.observe(s);s.classList.add('rf-reveal')});const reveal=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('rf-visible')}),{threshold:.08});sections.forEach(s=>reveal.observe(s));window.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){stopTour();setCurrent(current+1)}if(e.key==='ArrowLeft'){stopTour();setCurrent(current-1)}if(e.key.toLowerCase()==='v')voiceBtn.click()});document.addEventListener('visibilitychange',()=>{if(document.hidden){stopTour();if('speechSynthesis'in window)speechSynthesis.cancel()}});setCurrent(0,false)})();