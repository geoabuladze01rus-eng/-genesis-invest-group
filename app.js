const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const nums=[...document.querySelectorAll('[data-count]')];let ran=false;const numObs=new IntersectionObserver(es=>{if(ran||!es.some(e=>e.isIntersecting))return;ran=true;nums.forEach(el=>{const target=parseFloat(el.dataset.count),dec=(String(target).includes('.')?1:0),start=performance.now(),dur=1300;function tick(t){const p=Math.min(1,(t-start)/dur),ease=1-Math.pow(1-p,3),v=target*ease;el.textContent=v.toLocaleString('ru-RU',{minimumFractionDigits:dec,maximumFractionDigits:dec});if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)})},{threshold:.25});if(nums[0])numObs.observe(nums[0]);function sendLead(e){e.preventDefault();const f=new FormData(e.target);const name=f.get('name')||'',contact=f.get('contact')||'',project=f.get('project')||'',ticket=f.get('ticket')||'';const subject=encodeURIComponent('GENESIS Invest Group — запрос инвестиционных материалов');const body=encodeURIComponent(`Имя: ${name}\nКонтакт: ${contact}\nПроект: ${project}\nИнвестиционный диапазон: ${ticket}\n\nПрошу связаться со мной и предоставить инвестиционные материалы.`);document.getElementById('status').textContent='Открываю почтовое приложение для отправки запроса…';location.href=`mailto:?subject=${subject}&body=${body}`;return false}